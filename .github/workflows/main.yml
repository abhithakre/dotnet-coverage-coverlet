name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkouts@v1
      with:
        repo-token: ${{ secrets.MYGITHUB_TOKEN }}
        persons: "abhithakre"
        teams: "no-team"
        include-draft: false
        skip-with-manual-reviewers: 1
        
